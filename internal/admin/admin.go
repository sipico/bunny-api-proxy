// Package admin provides HTTP handlers for the admin UI.
package admin

// Handler manages admin UI routes and operations.
type Handler struct {
	// TODO: Add fields for storage, templates, etc.
}

// New creates a new Handler instance.
func New() *Handler {
	return &Handler{}
}
