// Package proxy implements the core HTTP proxy logic for forwarding requests to bunny.net.
package proxy

// Proxy handles proxying requests to the bunny.net API with permission checks.
type Proxy struct {
	// TODO: Add fields for configuration, HTTP client, etc.
}

// New creates a new Proxy instance.
func New() *Proxy {
	return &Proxy{}
}
